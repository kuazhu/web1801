1. 数据(state)
2. 模板(JSX)
3. 数据+模板->原始DOM节点
4. 数据改变, 数据+模板->新的DOM节点
5. 用新的DOM节点替换原始DOM节点

虚拟DOM DOM对应的JS对象

1. 数据(state)
2. 模板(JSX)
3. 数据+模板->原始DOM节点
4. 生成原始的虚拟DOM 
5. 数据改变, 生成新的虚拟DOM
6. 比较原始的虚拟DOM和新的虚拟DOM(diff算法),找出不同
7. 根据找出的不同来更新DOM节点